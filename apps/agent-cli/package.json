{
  "name": "@mobile-agent/agent-cli",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "bin": {
    "agent": "src/index.ts"
  },
  "scripts": {
    "build": "bunx --bun tsc -p tsconfig.build.json",
    "dev": "bun src/index.ts",
    "test": "bun test src",
    "typecheck": "bunx --bun tsc -p tsconfig.json"
  },
  "dependencies": {
    "@mobile-agent/agentd": "workspace:*",
    "@mobile-agent/agents": "workspace:*",
    "@mobile-agent/application": "workspace:*",
    "@mobile-agent/cli-adapters": "workspace:*",
    "@mobile-agent/domain": "workspace:*",
    "@mobile-agent/logging": "workspace:*",
    "@mobile-agent/persistence": "workspace:*",
    "@mobile-agent/tailscale": "workspace:*"
  },
  "devDependencies": {
    "@mobile-agent/test-support": "workspace:*",
    "@types/node": "^26.1.2",
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
