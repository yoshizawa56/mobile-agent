# The Web bundle does not contain a muximod endpoint. The first-run screen
# imports the connection profile from the pairing QR at runtime.
#
# Optional Web development overrides. The muximod endpoint is supplied by the
# connection profile or pairing QR at runtime.
# VITE_ALLOWED_HOSTS=your-machine.tailnet.ts.net
