{
  "name": "@muximo/muximod",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "bunx --bun tsc -p tsconfig.build.json",
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test src --pass-with-no-tests",
    "typecheck": "bunx --bun tsc -p tsconfig.json"
  },
  "dependencies": {
    "@muximo/infrastructure": "workspace:*"
  },
  "devDependencies": {
    "@muximo/test-support": "workspace:*",
    "@types/node": "^26.1.2",
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
